services:
  filmdatenbank:
    build: .
    container_name: filmdatenbank
    ports:
      - "8080:3000"
    environment:
      - TMDB_API_KEY=${TMDB_API_KEY}
    volumes:
      - fdb-data:/data
    restart: unless-stopped

volumes:
  fdb-data:
